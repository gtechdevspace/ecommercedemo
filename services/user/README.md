# User Service

Stores user profiles and roles (MongoDB).

Endpoints:
- GET /health
- (add user endpoints) POST /api/users etc.

Follow `services/_template/README.md` for required features and tests.
